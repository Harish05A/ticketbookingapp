
/**
 * Gemini Services Deprecated
 * As per request for a simple, professional UI focusing on core business logic.
 */

export async function getMovieAIPrediction(movieTitle: string) {
  // Mocking response to maintain backward compatibility without side effects
  return null;
}

export async function getAdminAnalyticsSummary(bookingsCount: number, revenue: number) {
  // No longer used in UI
  return "";
}
